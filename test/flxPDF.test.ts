import { before } from 'mocha';
import { expect } from 'chai';
import * as fs from 'fs';

describe('flxPDF', () => {
    it('should create a file when ArrayBuffer is provided', (done) => {
        
    });
});